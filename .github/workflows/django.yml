name: Django CI

on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Explore GitHub Actions"
      - run: echo "This is a simple workflow to explore GitHub Actions."
      - run: echo "You can add more steps to this workflow as needed."
      - name: Checkout code
        uses: actions/checkout@v2
      - run: echo "Code checked out successfully ${{ github.repository }}."
      - run: echo "This is a placeholder for your Django CI workflow."
      - name: Run Django tests
        run: |
          ls ${{ github.workspace }}
      - run: echo "Django tests completed is ${{ job.status }}."

